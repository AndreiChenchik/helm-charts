{{- if or .Values.vaultSecrets.toEnv.enabled .Values.vaultSecrets.toMount.enabled }}
apiVersion: v1
kind: ServiceAccount

metadata:
  name: "{{ .Release.Name }}"
  namespace: "{{ .Release.Namespace }}"
{{- end }}