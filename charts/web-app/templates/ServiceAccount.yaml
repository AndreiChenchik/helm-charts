{{- if or .Values.vault.envSecret.enabled .Values.vault.mountSecret.enabled }}
apiVersion: v1
kind: ServiceAccount

metadata:
  name: "{{ .Release.Name }}"
  namespace: "{{ .Release.Namespace }}"
{{- end }}
