{{- if .Values.config }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ .Release.Name }}"
  namespace: "{{ .Release.Namespace }}"
data: 
  {{- range $k, $v :=  .Values.config }}
  {{ $k }}: '{{ $v }}'
  {{- end }}
{{- end }}